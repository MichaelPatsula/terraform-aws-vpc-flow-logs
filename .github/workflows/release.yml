# name: Release

# on:
#   push:
#     tags:
#       - 'v*'
#   workflow_dispatch:
#     inputs:
#       version:
#         description: 'Version to release (e.g., v1.0.0)'
#         required: true
#         type: string

# env:
#   TF_VERSION: '1.6.0'

# jobs:
#   validate-release:
#     name: 'Validate Release'
#     runs-on: ubuntu-latest
#     outputs:
#       version: ${{ steps.version.outputs.version }}

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Get version
#       id: version
#       run: |
#         if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
#           echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
#         else
#           echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
#         fi

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Validate all examples
#       run: |
#         for example in examples/*/; do
#           echo "Validating $example"
#           cd "$example"
#           terraform init -backend=false
#           terraform validate
#           cd ../..
#         done

#   create-release:
#     name: 'Create Release'
#     runs-on: ubuntu-latest
#     needs: validate-release
#     permissions:
#       contents: write

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4
#       with:
#         fetch-depth: 0

#     - name: Generate changelog
#       id: changelog
#       run: |
#         # Get the previous tag
#         PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
        
#         if [ -n "$PREVIOUS_TAG" ]; then
#           echo "changelog<<EOF" >> $GITHUB_OUTPUT
#           git log --pretty=format:"- %s" ${PREVIOUS_TAG}..HEAD >> $GITHUB_OUTPUT
#           echo "EOF" >> $GITHUB_OUTPUT
#         else
#           echo "changelog=Initial release" >> $GITHUB_OUTPUT
#         fi

#     - name: Create Release
#       uses: actions/create-release@v1
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#       with:
#         tag_name: ${{ needs.validate-release.outputs.version }}
#         release_name: Release ${{ needs.validate-release.outputs.version }}
#         body: |
#           ## Changes in this Release
          
#           ${{ steps.changelog.outputs.changelog }}
          
#           ## Usage
          
#           ```hcl
#           module "vpc_flow_logs" {
#             source = "github.com/michaelpatsula/terraform-aws-vpc-flow-logs?ref=${{ needs.validate-release.outputs.version }}"
            
#             name   = "my-vpc"
#             vpc_id = "vpc-12345678"
            
#             log_destination_type = "cloud-watch-logs"
#             traffic_type         = "ALL"
#           }
#           ```
          
#           ## Examples
          
#           See the [examples directory](https://github.com/michaelpatsula/terraform-aws-vpc-flow-logs/tree/${{ needs.validate-release.outputs.version }}/examples) for usage examples.
#         draft: false
#         prerelease: ${{ contains(needs.validate-release.outputs.version, 'alpha') || contains(needs.validate-release.outputs.version, 'beta') || contains(needs.validate-release.outputs.version, 'rc') }}

#   terraform-registry:
#     name: 'Terraform Registry'
#     runs-on: ubuntu-latest
#     needs: [validate-release, create-release]
#     if: github.repository_owner == 'michaelpatsula'

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Terraform Registry Documentation
#       run: |
#         echo "## Terraform Registry Release Notes" >> $GITHUB_STEP_SUMMARY
#         echo "" >> $GITHUB_STEP_SUMMARY
#         echo "Version: ${{ needs.validate-release.outputs.version }}" >> $GITHUB_STEP_SUMMARY
#         echo "" >> $GITHUB_STEP_SUMMARY
#         echo "### Module Usage" >> $GITHUB_STEP_SUMMARY
#         echo '```hcl' >> $GITHUB_STEP_SUMMARY
#         echo 'module "vpc_flow_logs" {' >> $GITHUB_STEP_SUMMARY
#         echo '  source = "michaelpatsula/vpc-flow-logs/aws"' >> $GITHUB_STEP_SUMMARY
#         echo '  version = "${{ needs.validate-release.outputs.version }}"' >> $GITHUB_STEP_SUMMARY
#         echo '  ' >> $GITHUB_STEP_SUMMARY
#         echo '  name   = "my-vpc"' >> $GITHUB_STEP_SUMMARY
#         echo '  vpc_id = "vpc-12345678"' >> $GITHUB_STEP_SUMMARY
#         echo '}' >> $GITHUB_STEP_SUMMARY
#         echo '```' >> $GITHUB_STEP_SUMMARY

#   notify:
#     name: 'Notify Release'
#     runs-on: ubuntu-latest
#     needs: [validate-release, create-release]
#     if: always()

#     steps:
#     - name: Notify Success
#       if: needs.create-release.result == 'success'
#       run: |
#         echo "✅ Release ${{ needs.validate-release.outputs.version }} created successfully!"
#         echo "🔗 View release: https://github.com/${{ github.repository }}/releases/tag/${{ needs.validate-release.outputs.version }}"

#     - name: Notify Failure
#       if: needs.create-release.result == 'failure'
#       run: |
#         echo "❌ Release ${{ needs.validate-release.outputs.version }} failed!"
#         echo "Please check the workflow logs for details."
