# name: Test

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]
#   schedule:
#     # Run tests weekly on Sundays at 2 AM UTC
#     - cron: '0 2 * * 0'

# env:
#   TF_VERSION: '1.6.0'
#   AWS_REGION: 'us-west-2'

# jobs:
#   unit-tests:
#     name: 'Unit Tests'
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         example: [
#           'cloudwatch-basic',
#           's3-basic',
#           'existing-destination',
#           'advanced-configuration',
#           'complete-infrastructure'
#         ]

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Terraform Init
#       run: |
#         cd examples/${{ matrix.example }}
#         terraform init -backend=false

#     - name: Terraform Plan
#       run: |
#         cd examples/${{ matrix.example }}
#         terraform plan -var="aws_region=${{ env.AWS_REGION }}" -out=tfplan

#     - name: Terraform Show
#       run: |
#         cd examples/${{ matrix.example }}
#         terraform show -json tfplan > plan.json

#     - name: Validate Plan Output
#       run: |
#         cd examples/${{ matrix.example }}
#         # Check that the plan contains expected resources
#         if [ "${{ matrix.example }}" = "cloudwatch-basic" ]; then
#           jq -e '.planned_values.root_module.child_modules[0].resources[] | select(.type == "aws_flow_log")' plan.json
#           jq -e '.planned_values.root_module.child_modules[0].resources[] | select(.type == "aws_cloudwatch_log_group")' plan.json
#         elif [ "${{ matrix.example }}" = "s3-basic" ]; then
#           jq -e '.planned_values.root_module.child_modules[0].resources[] | select(.type == "aws_flow_log")' plan.json
#           jq -e '.planned_values.root_module.child_modules[0].resources[] | select(.type == "aws_s3_bucket")' plan.json
#         elif [ "${{ matrix.example }}" = "complete-infrastructure" ]; then
#           jq -e '.planned_values.root_module.resources[] | select(.type == "aws_vpc")' plan.json
#           jq -e '.planned_values.root_module.resources[] | select(.type == "aws_flow_log")' plan.json
#         fi

#   integration-tests:
#     name: 'Integration Tests'
#     runs-on: ubuntu-latest
#     if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: ${{ env.AWS_REGION }}

#     - name: Terraform Init
#       run: |
#         cd examples/cloudwatch-basic
#         terraform init

#     - name: Terraform Apply
#       run: |
#         cd examples/cloudwatch-basic
#         terraform apply -var="aws_region=${{ env.AWS_REGION }}" -auto-approve

#     - name: Verify Resources
#       run: |
#         cd examples/cloudwatch-basic
#         # Check that resources were created
#         terraform output

#     - name: Terraform Destroy
#       if: always()
#       run: |
#         cd examples/cloudwatch-basic
#         terraform destroy -var="aws_region=${{ env.AWS_REGION }}" -auto-approve

#   conformance-tests:
#     name: 'Conformance Tests'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Install conftest
#       run: |
#         wget https://github.com/open-policy-agent/conftest/releases/latest/download/conftest_linux_amd64.tar.gz
#         tar xzf conftest_linux_amd64.tar.gz
#         sudo mv conftest /usr/local/bin/

#     - name: Create conftest policies
#       run: |
#         mkdir -p policies
#         cat > policies/terraform.rego << 'EOF'
#         package terraform

#         # Ensure all resources have tags
#         deny[msg] {
#             resource := input.resource_changes[_]
#             resource.type != "data"
#             not resource.change.after.tags
#             msg := sprintf("Resource %s of type %s is missing tags", [resource.address, resource.type])
#         }

#         # Ensure VPC Flow Logs have proper configuration
#         deny[msg] {
#             resource := input.resource_changes[_]
#             resource.type == "aws_flow_log"
#             resource.change.after.traffic_type == "ALL"
#             resource.change.after.log_destination_type == "cloud-watch-logs"
#             not resource.change.after.log_destination
#             msg := "VPC Flow Log with ALL traffic should use S3 for cost optimization"
#         }
#         EOF

#     - name: Run conftest
#       run: |
#         for example in examples/*/; do
#           echo "Testing $example"
#           cd "$example"
#           terraform init -backend=false
#           terraform plan -var="aws_region=${{ env.AWS_REGION }}" -out=tfplan
#           terraform show -json tfplan > plan.json
#           conftest test plan.json --policy ../policies
#           cd ../..
#         done

#   performance-tests:
#     name: 'Performance Tests'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}

#     - name: Measure plan time
#       run: |
#         echo "## Performance Test Results" >> $GITHUB_STEP_SUMMARY
#         echo "" >> $GITHUB_STEP_SUMMARY
        
#         for example in examples/*/; do
#           echo "Testing $example"
#           cd "$example"
          
#           # Measure init time
#           start_time=$(date +%s)
#           terraform init -backend=false
#           init_time=$(($(date +%s) - start_time))
          
#           # Measure plan time
#           start_time=$(date +%s)
#           terraform plan -var="aws_region=${{ env.AWS_REGION }}" > /dev/null
#           plan_time=$(($(date +%s) - start_time))
          
#           echo "| $example | ${init_time}s | ${plan_time}s |" >> $GITHUB_STEP_SUMMARY
          
#           cd ../..
#         done
        
#         echo "" >> $GITHUB_STEP_SUMMARY
#         echo "| Example | Init Time | Plan Time |" >> $GITHUB_STEP_SUMMARY
#         echo "|---------|-----------|-----------|" >> $GITHUB_STEP_SUMMARY

#   test-summary:
#     name: 'Test Summary'
#     runs-on: ubuntu-latest
#     needs: [unit-tests, integration-tests, conformance-tests, performance-tests]
#     if: always()

#     steps:
#     - name: Test Summary
#       run: |
#         echo "## 🧪 Test Results Summary" >> $GITHUB_STEP_SUMMARY
#         echo "" >> $GITHUB_STEP_SUMMARY
        
#         if [ "${{ needs.unit-tests.result }}" = "success" ]; then
#           echo "✅ **Unit Tests**: All passed" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Unit Tests**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.integration-tests.result }}" = "success" ]; then
#           echo "✅ **Integration Tests**: Passed" >> $GITHUB_STEP_SUMMARY
#         elif [ "${{ needs.integration-tests.result }}" = "skipped" ]; then
#           echo "⏭️ **Integration Tests**: Skipped" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Integration Tests**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.conformance-tests.result }}" = "success" ]; then
#           echo "✅ **Conformance Tests**: Passed" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Conformance Tests**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.performance-tests.result }}" = "success" ]; then
#           echo "✅ **Performance Tests**: Completed" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Performance Tests**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
