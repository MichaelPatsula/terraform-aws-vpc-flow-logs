# name: Documentation

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]
#   workflow_dispatch:

# jobs:
#   validate-docs:
#     name: 'Validate Documentation'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'

#     - name: Install markdownlint
#       run: npm install -g markdownlint-cli

#     - name: Run markdownlint
#       run: markdownlint "**/*.md" --ignore node_modules

#     - name: Check for broken links
#       run: |
#         # Install link checker
#         npm install -g markdown-link-check
        
#         # Check all markdown files for broken links
#         find . -name "*.md" -not -path "./node_modules/*" -exec markdown-link-check {} \;

#   generate-terraform-docs:
#     name: 'Generate Terraform Docs'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Install terraform-docs
#       run: |
#         curl -sSLo terraform-docs https://github.com/terraform-docs/terraform-docs/releases/latest/download/terraform-docs-linux-amd64
#         chmod +x terraform-docs
#         sudo mv terraform-docs /usr/local/bin/

#     - name: Generate documentation
#       run: |
#         terraform-docs markdown table --output-file README.md --output-mode inject .

#     - name: Check for documentation changes
#       id: docs-check
#       run: |
#         if git diff --quiet; then
#           echo "changed=false" >> $GITHUB_OUTPUT
#         else
#           echo "changed=true" >> $GITHUB_OUTPUT
#         fi

#     - name: Commit documentation changes
#       if: steps.docs-check.outputs.changed == 'true'
#       run: |
#         git config --local user.email "action@github.com"
#         git config --local user.name "GitHub Action"
#         git add README.md
#         git commit -m "docs: update terraform documentation" || exit 0
#         git push

#   validate-examples:
#     name: 'Validate Example Documentation'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: '1.6.0'

#     - name: Validate example README files
#       run: |
#         for example_dir in examples/*/; do
#           if [ -f "${example_dir}README.md" ]; then
#             echo "✅ ${example_dir}README.md exists"
#           else
#             echo "❌ ${example_dir}README.md is missing"
#             exit 1
#           fi
#         done

#     - name: Check example file structure
#       run: |
#         for example_dir in examples/*/; do
#           echo "Checking $example_dir"
          
#           # Check required files
#           required_files=("main.tf" "variables.tf" "outputs.tf" "providers.tf" "data.tf" "README.md")
#           for file in "${required_files[@]}"; do
#             if [ -f "${example_dir}${file}" ]; then
#               echo "  ✅ ${file}"
#             else
#               echo "  ❌ ${file} is missing"
#               exit 1
#             fi
#           done
#         done

#   spell-check:
#     name: 'Spell Check'
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Install cspell
#       run: npm install -g cspell

#     - name: Create cspell config
#       run: |
#         cat > cspell.json << EOF
#         {
#           "version": "0.2",
#           "language": "en",
#           "words": [
#             "terraform",
#             "cloudwatch",
#             "aws",
#             "vpc",
#             "flowlogs",
#             "flowlogs",
#             "s3",
#             "iam",
#             "arn",
#             "cidr",
#             "subnet",
#             "gateway",
#             "nat",
#             "availability",
#             "zones",
#             "parquet",
#             "hive",
#             "partitioning",
#             "lifecycle",
#             "retention",
#             "infrequent",
#             "access",
#             "standard",
#             "delivery",
#             "analytics",
#             "monitoring",
#             "security",
#             "compliance",
#             "archival",
#             "optimization",
#             "performance",
#             "cost",
#             "optimization",
#             "michaelpatsula"
#           ],
#           "ignoreWords": [],
#           "import": []
#         }
#         EOF

#     - name: Run spell check
#       run: cspell "**/*.md" --config cspell.json

#   docs-summary:
#     name: 'Documentation Summary'
#     runs-on: ubuntu-latest
#     needs: [validate-docs, generate-terraform-docs, validate-examples, spell-check]
#     if: always()

#     steps:
#     - name: Documentation Summary
#       run: |
#         echo "## 📚 Documentation Status" >> $GITHUB_STEP_SUMMARY
#         echo "" >> $GITHUB_STEP_SUMMARY
        
#         if [ "${{ needs.validate-docs.result }}" = "success" ]; then
#           echo "✅ **Markdown Validation**: Passed" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Markdown Validation**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.generate-terraform-docs.result }}" = "success" ]; then
#           echo "✅ **Terraform Docs**: Generated" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Terraform Docs**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.validate-examples.result }}" = "success" ]; then
#           echo "✅ **Example Documentation**: Valid" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Example Documentation**: Invalid" >> $GITHUB_STEP_SUMMARY
#         fi
        
#         if [ "${{ needs.spell-check.result }}" = "success" ]; then
#           echo "✅ **Spell Check**: Passed" >> $GITHUB_STEP_SUMMARY
#         else
#           echo "❌ **Spell Check**: Failed" >> $GITHUB_STEP_SUMMARY
#         fi
